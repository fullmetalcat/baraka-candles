pluginManagement {
    repositories {
        gradlePluginPortal()
    }
    plugins {
        id 'com.github.johnrengelman.shadow' version '7.1.0'
        id 'org.unbroken-dome.test-sets' version '4.0.0'
    }
}

rootProject.name = 'candles'

include 'candles'